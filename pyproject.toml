[project]
name = "countries-info"
version = "0.1.0"
description = "Flask app to display country details from REST Countries"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "Flask==3.0.3",
  "requests==2.32.3",
]